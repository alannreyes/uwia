-- Use existing database
USE axioma;

-- Create user if not exists (adjust permissions as needed)
-- CREATE USER IF NOT EXISTS 'uwia_user'@'localhost' IDENTIFIED BY 'your_secure_password';
-- GRANT ALL PRIVILEGES ON uwia_db.* TO 'uwia_user'@'localhost';
-- FLUSH PRIVILEGES;